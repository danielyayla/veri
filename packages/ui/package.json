{
  "name": "@veri/ui",
  "version": "0.1.0",
  "description": "Local desktop UI for Veri knowledge bases: five screens over the veri/ directory",
  "type": "module",
  "private": true,
  "main": "dist/main.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && esbuild dist/renderer/app.js --bundle --format=esm --outfile=dist/renderer/app.bundle.js --log-level=warning",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"src/**/*.test.ts\"",
    "start": "electron ."
  },
  "dependencies": {
    "@codemirror/autocomplete": "^6.20.3",
    "@codemirror/commands": "^6.10.4",
    "@codemirror/lang-markdown": "^6.5.2",
    "@codemirror/language": "^6.12.4",
    "@codemirror/state": "^6.7.1",
    "@codemirror/view": "^6.43.8",
    "@lezer/highlight": "^1.2.3",
    "@veri/cli": "^0.1.0",
    "@veri/core": "^0.1.0",
    "@veri/mcp": "^0.1.0"
  },
  "devDependencies": {
    "electron": "^38.0.0",
    "esbuild": "^0.28.2"
  }
}
