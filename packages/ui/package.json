{
  "name": "@verikb/ui",
  "version": "0.6.0",
  "license": "Apache-2.0",
  "description": "Local desktop UI for Veri knowledge bases: a desktop lens over the veri/ directory",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && esbuild dist/renderer/app.js --bundle --format=esm --outfile=dist/renderer/app.bundle.js --log-level=warning && node scripts/assemble-frontend.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"src/**/*.test.ts\"",
    "start": "npm run build && tauri dev",
    "dist": "npm run build && node scripts/fetch-node.mjs && node scripts/stage-sidecar.mjs && tauri build"
  },
  "dependencies": {
    "@verikb/cli": "^0.1.0",
    "@verikb/core": "^0.1.0",
    "@verikb/mcp": "^0.1.0"
  },
  "devDependencies": {
    "@codemirror/autocomplete": "^6.20.3",
    "@codemirror/commands": "^6.10.4",
    "@codemirror/lang-markdown": "^6.5.2",
    "@codemirror/language": "^6.12.4",
    "@codemirror/search": "^6.7.1",
    "@codemirror/state": "^6.7.1",
    "@codemirror/view": "^6.43.8",
    "@lezer/highlight": "^1.2.3",
    "@tauri-apps/cli": "^2.11.4",
    "esbuild": "^0.28.2"
  }
}
