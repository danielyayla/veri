{
  "name": "veri",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run build && npm test --workspaces --if-present",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present",
    "build": "npm run build -w @veri/core && npm run build -w @veri/cli && npm run build -w @veri/mcp"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.2"
  }
}
