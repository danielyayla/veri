{
  "name": "veri",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run build && npm test --workspaces --if-present",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present",
    "build": "npm run build -w @verikb/core && npm run build -w @verikb/cli && npm run build -w @verikb/action && npm run build -w @verikb/mcp && npm run build -w @verikb/ui"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^7.0.2"
  }
}
