
(Describe the requirement.)

(Declare its epistemic kind in frontmatter — `kind: constraint` for what
must be satisfied and stay satisfied, `kind: hypothesis` for a bet tested
by shipping; absent means constraint. A hypothesis also declares the
outcome that would confirm or refute it, e.g.
`outcome: {metric: time-to-first-success, target: "< 5 minutes"}` —
`veri check` flags a hypothesis without one.)

## Acceptance criteria

- [ ] First criterion
